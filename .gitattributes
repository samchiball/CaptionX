# 모든 텍스트 파일의 줄바꿈을 LF로 통일한다.
# 저장소·체크아웃 모두 LF로 고정해 OS별 CRLF 차이로 인한 노이즈 diff를 막는다.
* text=auto eol=lf

# 바이너리 자산은 줄바꿈 변환 대상에서 제외한다.
*.png binary
*.jpg binary
*.ico binary
*.icns binary
*.woff binary
*.woff2 binary
*.ttf binary
*.bin binary
*.onnx binary
